The user profile page is throwing a 500 error intermittently in production but I can't reproduce it locally. Walk me through finding the root cause — build a feedback loop, reproduce it, form hypotheses, instrument the code, and fix it.
