import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Add project-specific configuration here.
  // Opt-in features: see _common/manual-setup-templates/ for Sentry, Supabase, etc.
};

export default nextConfig;
