---
name: hello
description: Smoke-test skill for plugin install verification. Use when the user types /hello to confirm the forge plugin is installed and reachable.
type: minimal
---

# Hello

Output exactly:

```
hello from forge — plugin install successful.
```

That is all. No further action.

> This skill exists only to verify `/plugin install forge` works end-to-end. Will be removed in a later cleanup (likely EPIC A #11 or first post-bootstrap maintenance).
