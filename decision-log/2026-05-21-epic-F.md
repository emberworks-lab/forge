# Decision log — EPIC F (#39) Caveman integration pilot

Run: 2026-05-21 overnight autonomous
Branch: `feature/forge-39-caveman-integration-pilot`

---

## [#40 F.1] Pilot 6 paired sessions cannot be performed autonomously

**Epic:** EPIC F (#39)
**Ticket:** #40 — Pilot: run /caveman in 6 real sessions, measure token diff
**Context:** Ticket asks to run `/caveman` paired (with vs without) on 6 representative real sessions (`forge:execute-ticket` ×2, `forge:brainstorm`, `forge:diagnose`, `forge:create-epic`, casual Q&A), measure token diff via telemetry or manual UI inspection, score debuggability.

**Problem:** This is a human-driven empirical pilot. I am running unattended overnight. I cannot:
- Open 12 Claude Code sessions (6 paired) on real workloads
- Observe my own UI token counters from outside
- Score "debug-clarity" on my own output (self-evaluation is biased and not what the ticket asks for)

Faking 6 sessions of pilot data with synthesized numbers would corrupt downstream F.2 policy thresholds and mislead future readers. The ticket says "honest narrative" — fabricated numbers are dishonest.

**Options considered:**
- **A:** Synthesize plausible numbers from `/caveman` published claims (~75% → 7-22% realistic per epic body) and write fake pilot — quick close, dishonest output, corrupts F.2 policy
- **B:** Skip ticket entirely, leave file absent — F.2/F.3/F.4 lose the artifact they depend on
- **C:** Write `forge/docs/caveman-pilot-2026-05.md` as a **template + execution protocol** ready for human execution, mark ticket as "deferred — requires human-driven pilot", post comment on ticket explaining, reopen later when user runs the pilot — F.2 then ships with theoretical thresholds clearly marked as placeholder
- **D:** Close epic F entirely as "blocked — needs human pilot" and skip B/C+D/G/H rebuilds — wastes the rest of the night

**Chosen:** **C**
**Why:** Preserves the artifact's purpose (a reusable pilot protocol), honest about what wasn't measured, allows F.2/F.3/F.4 to ship in a coherent "pre-calibration" state, lets user run the actual pilot in 30-60 min of their own work without re-doing scaffolding.

**May need revisit if:** User does run the pilot — then F.2 thresholds need calibration commit and F.3 may re-open with concrete recommendations.

---

## [#41 F.2] Policy doc thresholds with no pilot data yet

**Epic:** EPIC F (#39)
**Ticket:** #41 — Decision matrix policy doc (forge/docs/conventions/caveman-policy.md)
**Context:** Ticket says draft a decision policy "based on F.1 pilot data" mapping skill type → opt-in/skip with thresholds, "references concrete F.1 numbers".

**Problem:** F.1 was deferred (see above). No concrete numbers exist.

**Options considered:**
- **A:** Skip ticket, wait for F.1 — leaves F.3/F.4 dangling and blocks epic close
- **B:** Write policy with theoretical thresholds drawn from `/caveman` published claims + first-principles reasoning, clearly mark all numbers as `PROVISIONAL — calibrate after F.1`, include a "How to recalibrate" section the user can run after the real pilot
- **C:** Write policy with empty thresholds only, all `TBD` — useless artifact, no decision shape

**Chosen:** **B**
**Why:** Provisional thresholds give F.3/F.4 something to reference and shape future decisions; explicit `PROVISIONAL` markers prevent the doc being mistaken for measured truth; "How to recalibrate" section makes the F.1 follow-up actionable.

**May need revisit if:** F.1 pilot returns numbers that contradict the theoretical band (most likely if a workload sits in the long tail).

---

## [#42 F.3] No integration warranted pre-calibration

**Epic:** EPIC F (#39)
**Ticket:** #42 — Targeted integration in 1-2 skills (if matrix recommends)
**Context:** Ticket is **explicitly conditional**: "If F.2 matrix recommends opt-in for ≥1 skill, patch those skills. If F.2 matrix recommends NONE — close with 'no integration warranted'."

**Problem:** F.2 ships with provisional thresholds, not calibrated recommendations. Patching a skill (e.g. `forge:execute-epic`) to auto-propose `/caveman` based on uncalibrated thresholds risks user-facing noise without measured ROI.

**Options considered:**
- **A:** Patch `forge:execute-epic` per the F.2 spec anyway, treating "most likely candidate" as enough signal — premature, breaks the "pilot first, decide second" principle stated in the epic Goal
- **B:** Close ticket with "no integration warranted **until F.1 pilot returns**", document the would-be integration pattern in F.4 so it's ready to reuse — honest, preserves option value
- **C:** Build a feature-flag-gated patch (off by default, on after pilot) — adds complexity for unclear win

**Chosen:** **B**
**Why:** Epic explicitly framed as "pilot first, decide second". Honoring that means not landing user-visible integration based on a placeholder matrix. F.4 still ships the pattern so future calibration can wire it in by reference.

**May need revisit if:** F.1 pilot shows ≥40% economy on a skill — then re-open this ticket and patch.

---

## [#43 F.4] Add caveman opt-in pattern section to forge:writing-skill

**Epic:** EPIC F (#39)
**Ticket:** #43 — Add caveman opt-in pattern section to forge:writing-skill
**Context:** Add a "Caveman opt-in pattern" section + audit.sh rule update.

**Problem:** No real contention here. F.4 is documentation-shaped and doesn't depend on numerical pilot data — only on the pattern shape. The audit.sh enhancement question is the only fork:

**Options considered:**
- **A:** Add a new audit.sh lint rule "caveman opt-in declared if skill mentions /caveman in body" — false positives are noisy (mentioning ≠ opting in)
- **B:** Skip audit.sh enhancement; rely on reference content + future code-review to catch violations — less mechanical, but the pattern is rare enough that automation isn't worth its false-positive cost
- **C:** Add audit.sh rule with strict regex (only flags `/caveman on` + `/caveman off` not balanced in the same SKILL.md) — narrow, useful

**Chosen:** **B**
**Why:** The ticket says "enhancement (if any) tested on forge:execute-epic patched in F.3" — and F.3 was closed without patching. With no concrete skill to test the rule against, automating it now is YAGNI. Section + anti-pattern callout are enough.

**May need revisit if:** Multiple skills opt in to caveman → false-positive cost of a rule drops below the manual-review cost; revisit option C.

---

## Epic-level summary

EPIC F ships **partial** in a coherent way:
- F.1 deferred with clear protocol for human execution
- F.2 shipped provisional, ready for recalibration
- F.3 closed as "no integration pre-pilot" (per epic's own conditional clause)
- F.4 shipped clean (documentation only)

Once user runs the real pilot, F.1/F.2/F.3 form a natural follow-up mini-epic.

---

## [epic-close] Path selection + skipped gates

**Context:** `forge:epic-close` Step 1 decision tree presents A (merge) / B (draft PR) / C (cleanup-only). Step 3a (simplify-branch), Step 3a.5 (ultrareview) are optional follow-ups.

**Decisions (all per user's standing instructions):**

| Gate | Choice | Why |
|---|---|---|
| Tests-pass hard gate | PASSED via `audit.sh` on all SKILL.md files | No project-level test suite; audit is the only mechanical gate; all 3 changed SKILL.md exit 0 |
| Manual test confirmation | Auto-y | User explicit: "не чекай мене" |
| Path | **A — merge to main (squash)** | User explicit: "PR з auto-merge у main" |
| Authorization gate | Auto-authorized | User granted `--dangerously-skip-permissions` + explicit "сам мерж в мейн, довіряю" |
| `forge:simplify-branch` | SKIP | Tiny diff (2 new docs, 1 reference, 1 SKILL.md insertion, 1 version bump). Simplify overhead > benefit for ~290 lines of new prose |
| `/ultrareview` | SKIP | User explicit: "без ultrareview" |
| Status update | SKIP | Repo has no FEATURES.md project doc; epic body unchanged |

**May need revisit if:** Future pilot reveals provisional thresholds were wildly off — re-open #41 and #42.
