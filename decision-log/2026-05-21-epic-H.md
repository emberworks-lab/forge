# Decision log — EPIC H (#78) Manual testing automation (web pilot + mobile research)

Run: 2026-05-21 overnight autonomous
Branch: `feature/forge-78-manual-testing-automation`

---

## [discovery] `forge:e2e` backend skill does NOT exist

**Context:** Multiple EPIC H tickets ("#80 forge:e2e-web on the same pattern as forge:e2e", "#85 web e2e gate parallel to backend e2e gate") reference a backend `forge:e2e` skill as the reference shape. Searched `plugins/forge/skills/` — no such skill.

**Problem:** Either:
- The backend e2e skill is supposed to land in a different epic (it's not in EPIC B; nothing in C+D either), OR
- The backend "e2e" is just an ad-hoc convention without a dedicated skill (e.g. `*.e2e-spec.ts` files run via `test-runner` agent)

**Chosen:** **Build `forge:e2e-web` standalone, design its own pattern.** Don't block on the missing backend skill. The pattern: `*.e2e-web.spec.ts` files, opt-in via `<project>/.claude/e2e-web.json`, runs via existing `test-runner` agent with `type=e2e-web` flag. Future `forge:e2e` (backend equivalent) can mirror this shape.

**May need revisit if:** Someone defines a different pattern for `forge:e2e` later — this skill's interface may need realignment.

---

## [#79] Web project audit — what to audit

**Context:** Ticket says "Audit existing web projects (current test setup)". User's local projects: reddit-idea-forge, embergard-rpg, PantryPal, scaffold-tryout, VolatiLens.

**Problem:** Audit needs to read these external projects to assess. Subagents have filesystem access.

**Chosen:** **Audit reddit-idea-forge (the source for `web-nextjs` template)** since that's the primary web project. Skim the others briefly. Document what each has (Playwright? Jest? nothing?) + what's missing.

---

## [orchestration] 11-ticket triage

| Ticket | Decision | Notes |
|---|---|---|
| #79 audit web projects | DO | Subagent reads external repo |
| #80 forge:e2e-web skill | DO | Design pattern from scratch (no `forge:e2e` to mirror) |
| #81 playwright manual-setup template | DO | Doc only |
| #82 web-nextjs opt-in playwright module | DO | Extends template from EPIC C+D #57 |
| #83 create-ticket web e2e ack-criteria | DO | Parallel to EPIC B #48 (web variant) |
| #84 execute-ticket web TDD loop | DO | Parallel to EPIC B #49 (web variant) |
| #85 epic-close web e2e gate | DO | Wires #80 into epic-close Step 0 |
| #86 mobile e2e frameworks survey | DO | Research; web search |
| #87 mobile decision matrix per stack | DO | Synthesis from #86 |
| #88 Flutter+Patrol pilot design | DO | Design doc only (no implementation) |
| #89 Maestro pilot design | DO | Design doc only |

Net: 11/11 attempted. None deferred (all are doc/skill work, no required external pilots).

---

## [planning] Wave structure

```
Wave 1 (parallel): #79, #80, #81, #86          (foundational, independent)
Wave 2 (parallel): #82, #83, #87               (build on Wave 1 + EPIC C+D #57)
Wave 3 (parallel): #84, #85, #88, #89          (build on Wave 2)
```

3 waves, max 4 subagents per wave.

---
